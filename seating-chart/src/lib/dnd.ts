/** dataTransfer MIME type for dragging a student (from the assignments panel)
 *  onto a seat on the canvas. Shared by the drag source + the drop target. */
export const DND_STUDENT = "application/x-seating-student";
